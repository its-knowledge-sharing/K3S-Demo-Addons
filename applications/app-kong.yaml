apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: kong
spec:
  generators:
  - clusters:
      selector:
        matchLabels:
          custom: "true"
  template:
    metadata:
      name: 'kong-{{name}}'
    spec:
      project: "default"
      source:
        repoURL: https://github.com/its-software-services-devops/promrub-argocd-apps.git
        targetRevision: "{{name}}" #Based on environment
        path: 'manifests/kong'
        helm:
          ignoreMissingValueFiles: true
          valueFiles:
            - values.yaml
            - values-{{name}}.yaml
      destination:
        name: in-cluster
        namespace: kong
      syncPolicy:
        automated:
          selfHeal: true
          prune: true
        syncOptions:
          - CreateNamespace=true
